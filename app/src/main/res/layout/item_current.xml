<?xml version="1.0" encoding="utf-8"?><!--
  Copyright (c) 2019 . Wilberforce Uwadiegwu. All Rights Reserved.
  -->

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.text.format.DateUtils" />

        <import type="java.util.concurrent.TimeUnit" />

        <import type="com.jadebyte.jadeplayer.R" />

        <variable
            name="song"
            type="com.jadebyte.jadeplayer.main.songs.Song" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/selectableItemBackground"
        android:paddingStart="@dimen/padding_x_large"
        android:paddingTop="@dimen/padding_medium"
        android:paddingEnd="@dimen/padding_x_large"
        android:paddingBottom="@dimen/padding_medium">

        <TextView
            android:id="@+id/songTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="@{song.title}"
            android:textAppearance="@style/TextAppearance.AppCompat.Body1"
            android:textColor="@{song.current? R.color.colorAccent : R.color.textColorPrimary}"
            app:layout_constraintBottom_toTopOf="@+id/songArtist"
            app:layout_constraintEnd_toStartOf="@+id/songDuration"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Midnight Dogs" />

        <TextView
            android:id="@+id/songArtist"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="@{song.album.artist}"
            android:textAppearance="@style/TextAppearance.AppCompat.Caption"
            android:textColor="@{song.current? R.color.colorAccent : R.color.textColorSecondary}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="@+id/songTitle"
            app:layout_constraintRight_toRightOf="@+id/songTitle"
            app:layout_constraintTop_toBottomOf="@+id/songTitle"
            tools:text="Beyoncee" />


        <TextView
            android:id="@+id/songDuration"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="@{DateUtils.formatElapsedTime(TimeUnit.MILLISECONDS.toSeconds(song.duration))}"
            android:textAppearance="@style/TextAppearance.AppCompat.Caption"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="04:57 " />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>